<br>
Title:<br>
<%= theme.name%><br>

<% theme.photos.each do |x| %>
  <%= image_tag x.url[0] %>
<% end %>
<br>
System:
<br>
<% theme.systems.each do |system| %>
  <%= system.name %><br>
<% end %>

Views :
<%= theme.views %><br>
Comments :
<br>
<%= link_to "like", like_path(theme.id)%>
<%= theme.likes.length %>|<br>
<%= link_to "dislike", dislike_path(theme.id) %>
<%= theme.dislikes.length %>
<hr>
<br>
