<br>
Title:
<%= theme.name%><br>

<% theme.photos.each do |x| %>
  <%= image_tag x.url[0] %>
<% end %>

System:
<% theme.systems.each do |system| %>
<%= system.name %><br>
<% end %>

Views :
<%= theme.views %><br>

<%= link_to "More", theme_path(theme.id)%>
<br>
