<hr><br><%= render @categories %><br>
<hr>
<% if @themes == [] %>
  <% if user_signed_in? %>
    <%= link_to "Add your Theme!", new_theme_path%>
  <% else %>
    Want to create a new theme?<br>
    <%= link_to "Login", new_user_session_path %>
    or
    <%= link_to "Registration", new_user_registration_path %>

  <% end %>
<% else %>
  <%=render @themes%>
<% end %>
